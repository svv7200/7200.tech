# 7200.tech — IT-аутсорсинг

Сайт-визитка моего IT-аутсорсинг сервиса. Помогаю бизнесу и частным лицам решать технические задачи: от настройки серверов и сетевой инфраструктуры до разработки и поддержки веб-проектов.

## Стек сайта

- HTML5 / CSS3
- [Materialize CSS](https://materializecss.com/) — UI-фреймворк
- Кастомные шрифты: Mr. Amazing, Roboto

## Структура проекта

```
├── index.html          # Главная страница
└── assets/
    ├── css/            # Стили (Materialize + кастомные)
    ├── fonts/          # Шрифты
    └── files/          # Дополнительные файлы
```

---

## 🖥️ Инфраструктура VPS

**Сервер:** Германия, Debian 12  
**Панель управления VPN:** [3x-ui](https://github.com/MHSanaei/3x-ui) v3.2.8 (xray-core 26.6.1)

### Архитектура

```
Клиент
  │
  ├─► :443 TCP ──► xray (VLESS + XTLS-Vision)
  │                  └─► fallback ──► nginx :8080 ──► сайт 7200.tech
  │
  ├─► :4443 UDP ──► xray (Hysteria2 + QUIC)
  │
  └─► :51820 UDP ──► xray (WireGuard)
```

### Веб-сервер

Nginx слушает на порту `:8080` и отдаёт сайт-заглушку.  
Порт `:80` делает редирект на HTTPS.  
Браузерный трафик на `:443` xray передаёт в nginx через fallback — для антидетекта.

- Домен: [7200.tech](https://7200.tech)
- Веб-сервер: nginx 1.22
- Сертификат: Let's Encrypt (wildcard `*.7200.tech`)

---

## 🔒 VLESS + XTLS-Vision

Основной протокол. Работает на порту `443/TCP` поверх TLS — выглядит как обычный HTTPS.

**Параметры подключения:**

| Параметр | Значение |
|----------|----------|
| Протокол | VLESS |
| Адрес | `7200.tech` |
| Порт | `443` |
| TLS | Включён (`security=tls`) |
| Flow | `xtls-rprx-vision` |
| Fingerprint | `chrome` |
| SNI | `7200.tech` |

**Поддерживаемые клиенты:** Hiddify, v2rayN, NekoBox, Karing, Streisand (iOS)

---

## ⚡ Hysteria2

Протокол на базе QUIC (UDP). Быстрее VLESS на нестабильных соединениях, особенно на мобильных сетях.

**Параметры подключения:**

| Параметр | Значение |
|----------|----------|
| Протокол | Hysteria2 |
| Адрес | `7200.tech` |
| Порт | `4443` |
| TLS | Включён |
| ALPN | `h3` |
| Fingerprint | `chrome` |
| SNI | `7200.tech` |

**Формат ссылки:**
```
hysteria2://<password>@7200.tech:4443?alpn=h3&fp=chrome&security=tls&sni=7200.tech
```

**Поддерживаемые клиенты:** Hiddify, Karing, NekoBox, v2rayN 7+

> **Важно:** протокол работает по UDP. Порт 4443 должен быть открыт для UDP в файрволе.

---

## 🛡️ WireGuard

Протокол для постоянного подключения уровня сети. Идеально подходит для роутеров — весь трафик домашней сети идёт через VPN без настройки на каждом устройстве.

**Параметры сервера:**

| Параметр | Значение |
|----------|----------|
| Адрес сервера | `31.59.58.208:51820` |
| Протокол | UDP |
| Адресация клиентов | `10.0.0.2/32`, `10.0.0.3/32`, ... |
| MTU | 1420 |
| Keepalive | 25 сек |

### Как создать нового клиента WireGuard

1. Сгенерировать пару ключей (на сервере):
   ```bash
   CLIENT_PRIV=$(wg genkey)
   CLIENT_PUB=$(echo $CLIENT_PRIV | wg pubkey)
   echo "Приватный: $CLIENT_PRIV"
   echo "Публичный: $CLIENT_PUB"
   ```

2. В панели 3x-ui добавить нового пира в инбаунд WireGuard:
   - `publicKey` — публичный ключ клиента
   - `privateKey` — приватный ключ (для хранения в панели)
   - `allowedIPs` — следующий свободный IP, например `10.0.0.3/32`

3. Передать клиенту конфиг-файл `client.conf`:
   ```ini
   [Interface]
   PrivateKey = <приватный ключ клиента>
   Address = 10.0.0.X/32
   DNS = 1.1.1.1

   [Peer]
   PublicKey = TcZtYXbjd0Z3DeOzQc5ShgltnY13UaHN+dyGrdRiUUE=
   Endpoint = 31.59.58.208:51820
   AllowedIPs = 0.0.0.0/0
   PersistentKeepalive = 25
   ```

### Подключение на роутере TP-Link

1. Войти в веб-интерфейс роутера
2. Перейти: **VPN → WireGuard → Import config**
3. Загрузить файл `client.conf`
4. Нажать **Enable**

После подключения весь трафик домашней сети идёт через сервер в Германии.

**Поддерживаемые устройства:** TP-Link с поддержкой WireGuard, OpenWrt, Mikrotik 7+, Keenetic

---

## 📦 Подписка (Subscription)

Одна ссылка содержит сразу все протоколы клиента (VLESS + Hysteria2). Клиент сам выбирает оптимальный.

**Формат ссылки:**
```
https://7200.tech:2096/sub/<sub_id>
```

Ссылку для каждого пользователя можно найти в панели 3x-ui или скопировать в разделе клиента.

**Поддерживаемые клиенты:** Hiddify, Karing, NekoBox, v2rayN, Streisand

---

## ⚙️ Управление панелью

| Ресурс | Адрес |
|--------|-------|
| Панель 3x-ui | `https://31.59.58.208:9443/<basepath>/` |
| Файлы сайта на сервере | `/var/www/7200.tech/` |
| База данных панели | `/etc/x-ui/x-ui.db` |
| Сертификаты | `/root/cert/7200.tech/` |

Обновление файлов сайта — через SFTP (FileZilla):
- Хост: `31.59.58.208`, порт `22`, протокол `SFTP`
- Папка на сервере: `/var/www/7200.tech/`

---

## Контакт

Telegram: [@svv7200](https://t.me/svv7200)
